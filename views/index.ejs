<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <style>
@keyframes expandX {
  from {
    background-size: 0% 100%;
    opacity: 1.5;
  }
  to {
    background-size: 100% 100%;
    opacity: 1.5;
  }
}


.animate-text {
  background-image: linear-gradient(to right, #5eead4, #5eead4); /* Tailwind teal-300 */
  background-repeat: no-repeat;
  background-size: 0% 100%;
  background-clip: text;
  -webkit-background-clip: text;
  color: transparent;
  animation: expandX 3.5s linear infinite;
}

      </style>
</head>
 <header class="bg-zinc-900 ">
    <h1 class=" text-5xl font-sans font-bold  text-center m-0 p-0 animate-text">To Do List..</h1>
 </header>
<body>
     <div class="main w-full min-h-screen bg-zinc-900 ">
        <div class="form ml-5 mr-5">
            <form action="/create" method="POST">
                <input class="text-teal-300 w-full mt-10 rounded px-5 py-2 bg-zinc-800 rouded-md" type="text" name="title"
                    placeholder="Enter your task..." autofocus />

                <input class="mt-4  bg-teal-300 rounded-sm px-5 py-2 cursor-pointer" type="submit" value="Add" />
            </form>
        </div>
        <div class="w-full flex flex-wrap ">

            <% if(files.length>0 ){%>
                <% files.forEach(function(val){ %>
                    <div class="mt-7 ml-5 flex  task w-90 px-3 py-4 bg-teal-300 p-10 rounded-2xl">
                        <h3 class=" text-3xl tracking-tighter w-[80vw] "><%= val %> </h3>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <h2 class="text-white ml-7 mt-7">No Notes added yet..</h2>
                            <% } %>
        </div>
    </div>

</body>

</html>